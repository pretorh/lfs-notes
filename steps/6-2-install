# GETTEXT, SYSTEMD

systemd-229
    some tests will fail (depends on kernel config)
    cat ./test-suite.log | grep FAIL
    had these failings:
        test-path-util
        test-calendarspec
        test-copy
        test-dnssec
        test/udev-test.pl

# PACKAGES 7

- dbus
