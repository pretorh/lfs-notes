cd /sources

# PACKAGES 1

linux api headers (in linux-4.4.2.tar.xz)
man-pages
glibc
see 6-2a-glibc for more details and post install config

# ADJUSTING TOOLCHAIN
see 6-2b-toolchain

# PACKAGES 2

zlib
file
# verify PTYs are working
expect -c "spawn ls" | grep "spawn ls" && echo "SUCCESS" || echo "FAILED"
binutils
    The test 'Link with zlib-gabi compressed debug output' is known to fail
gmp
    all 190 tests must pass
mpfr
    all tests must pass
mpc

# GLIBC

takes realy long
see 6-2c-gcc for notes
